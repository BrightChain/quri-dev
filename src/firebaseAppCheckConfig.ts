import { IFirebaseAppCheckConfig } from './interfaces';

/* for local development: please fill in the siteKey below and do not commit it */

export const firebaseAppCheckConfig: IFirebaseAppCheckConfig = {
  siteKey: '',
};
